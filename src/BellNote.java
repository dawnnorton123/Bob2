
class BellNote {
	final Note1 note;
	final NoteLength length;

	// BellNote constructor //holds two things, this note.length and this.note
	BellNote(Note1 note, NoteLength length) {
		this.note = note;
		this.length = length;
	}
}